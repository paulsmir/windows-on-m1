# Paging Patch routing plan

**Goal:** accept production paging packets at their required pre-submit Patch DDI.
**Architecture:** FULL GRAPHICS WDDM3.0, existing paging encoder/records/executor.
Paging plans already contain resolved local offsets; no AGX relocation exists
in the16-byte paging marker. Patch validates only, does not execute or complete.
**Spec:** EXP499 exactPATCHargs plus Microsoft paging Patch contract.
**Execution:** inline, current GPU worktree, no newarchitecture approval.

## Source-first evidence and ownership

Dump9967060355454697b134efba0d20603ab93e92c22b5aba7e31214d4f7ad09ca2:
119/103 generated byVidSchiSubmitPagingCommand afterDdiPatch returnsC000000D,
notSubmitCommand. Arg3=ffffc901508bcbd8 isDXGKARG_PATCH (earlierSUBMIT decode
was wrongtype and mustnotbe used). Native499privatePDB decodes Flags1, fence1,
engine0, hContextffffcb83e96afde0, DMAsegment1/address160fffe000, size4096,
start0/end16; private4096/start0/end72; bothlistsNULL/alllengths0.

Inspected gdi_windows.c AdmissionDdiPatch: GDI-only8192private/nonemptylists/
Flags0 guards reject this. paging_windows.c AdmissionEncodePaging creates16-byte
marker plus72-byte private record and advances both pointers. render_paging.c
AdmissionPagingRecordsValid validates existing recordheader/count/kind.
Physical paging planner/executor and memory runtime retain all translation,
range checks and operation execution. No changes to these GREEN implementations.

Microsoft DXGKARG_PATCH documents NULL/zero allocation andpatchlists forpaging,
independentprivate submission offsets, and optionalNULLhContext forpowerpaging.
DXGK_PATCHFLAGS.Paging=1; Patch runsPASSIVE and may be repeated. This iscommon
FULL GRAPHICS behavior, not KMDOD admission. Native477/Mu406/Asahi contracts are
unchanged: patch doesnot touchMMIO/UAT/firmware/IRQ/queues.
https://learn.microsoft.com/en-us/windows-hardware/drivers/ddi/d3dkmddi/ns-d3dkmddi-_dxgkarg_patch
https://learn.microsoft.com/en-us/windows-hardware/drivers/ddi/d3dkmddi/ns-d3dkmddi-_dxgk_patchflags
https://learn.microsoft.com/en-us/windows-hardware/drivers/ddi/d3dkmddi/nc-d3dkmddi-dxgkddi_patch

## Task

Files: gdi_windows.c; tests/test_apple_agx_render_paging_patch.py.

- [ ] RED execute actualPatchdispatch/initialguards with exact499shape and
  productionencoder; currentGDI-onlyguards reject validpaging. No invented
  standalone memoryimplementation. Hostshim stops at unchangedGDIhandoff.
- [ ] Add staticAdmissionPatchPaging and dispatch beforeGDIguards forPaging.
  RequireadapterStarted, Flags exactlyPaging, engine0/nonzerofence; null/zero
  lists, bounded nonempty pairedranges, record/marker alignment/count relation.
  Optionalnonnullcontext must havevalidmagic/device/adapter ownership, noGDIflag
  requirement. AllowdocumentedNULLpagingcontext.
- [ ] Reuse AdmissionPagingRecordsValid, compare eachDMAmarker against its
  recordheader, leaveallbytes unchanged. No addresses to patch in this format.
  Do not change SubmitCommand/fences/worker/encoder or execution readiness.
- [ ] Verify batchslice/repeatedPatch is idempotent, malformedheader/count/
  bounds/alignment/list/flags/context rejected withoutwrites; rerunrender/feature
  suites, diffreview andcommit onlycoherentfix/test/plan, exactCHANGESrow.
- [ ] Freeze499pluscommittedfiles, pinnedFRYZZING500 fullbuild/sign/hashgates.
  Preregisteronehardwarebind: validpagingPatch ->actualSubmit/worker/completion
  ornextidentifiedfailure. Collectreceipts/ETL/dump beforeexactcleanup/ordinary.

Self-review: sourceofphysicaltranslation remainsproductionplanner; Patchdoesnot
pretendAGXexecution. Singlechangedcontract is distinguishingpaging fromGDI DMA.
Knownrecovery377/392,385emergencyonly. No capability orplatformABI changes.
